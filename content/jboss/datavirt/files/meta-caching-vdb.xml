<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<vdb name="CacheMetadataVDB" version="1">
    <description>cache-metadata VDB</description>


    <!--
	cache-metadata

	Can be "true" or "false". If "false", JBoss Data Virtualization will obtain metadata once 
        for every launch of the VDB. "true" will save a file containing the metadata into the 
        EAP_HOME/MODE/data directory. Defaults to "false" for -vdb.xml deployments otherwise "true". 
    -->
    <property name="cache-metadata" value="true"/>

    <model name="Accounts">
        <!--
          JDBC Import settings

          importer.useFullSchemaName directs the importer to drop the source
          schema from the Teiid object name, so that the Teiid fully qualified name
          will be in the form of <model name>.<table name>
        -->
        <property name="importer.useFullSchemaName" value="false"/>

         <!--
            This connector is defined to reference the Mysql local DS"
          -->
        <source name="mysql-connector" translator-name="translator-mysql" connection-jndi-name="java:jboss/AccountsDS"/>
    </model>

    <translator name="translator-mysql" type="mysql5">
        <property name="SupportsNativeQueries" value="true"/>
    </translator>

</vdb>

