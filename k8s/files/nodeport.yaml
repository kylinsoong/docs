apiVersion: v1
kind: Service
metadata:
  labels:
    app: app
  name: app-svc-nodeport
  namespace: test001
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    nodePort: 30080
  selector:
    app: app
  sessionAffinity: None
  type: NodePort
